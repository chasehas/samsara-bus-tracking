version: '3.8'

services:
  bus-tracker:
    build: .
    container_name: school-bus-tracker
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./config.json:/app/config.json:ro
      - ./logs:/app/logs
